import type { IconAliases } from './icons';

const aliases: IconAliases = {
  timer: 'mdi-timer',
  window_maximize: 'mdi-fullscreen',
  window_restore: 'mdi-fullscreen-exit',
};

export { aliases };
